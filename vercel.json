{
    "builds": [
        {
            "src": "next.config.ts",
            "use": "@vercel/next"
        }
    ],
    "ignoreCommand": "echo 'Ignore backend build'",
    "installCommand": "npm install",
    "buildCommand": "npm run build"
}